[project]
name = "review-dbt-microbatch"
version = "0.1.0"
description = "Example dbt project for processing review micro-batches"
readme = "README.md"
requires-python = ">=3.11,<3.12"
dependencies = [
    "dbt-core>=1.11.6",
    "dbt-databricks>=1.11.6",
]
